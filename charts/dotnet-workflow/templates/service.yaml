apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-workflow.fullname" . }}
  labels:
    {{- include "dotnet-workflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-workflow.selectorLabels" . | nindent 4 }}
